# 风险关系图说明

## 主对象与来源

`risk_records` 是风险主对象，来源分两类：

- 人工风险：`creationMode=manual`、`taskId=null`，通过 `discoverySource`、`discoverySourceDetail`、
  `sourceReference` 记录真实来源，不写 `risk_sources`，必须指定 `reviewerUserId`。
- 评估风险：`creationMode=evaluation|finding_escalation`、`taskId` 非空、
  `discoverySource=compliance_assessment`。评估单元关系写入 `risk_sources`；已有不符合项时同时写入
  `risk_finding_links`，使风险接受或关闭能够解决对应不符合项。

两类风险都必须通过 `risk_affected_assets` 关联至少一个有效资产。整改行动通过
`risk_action_links` 与一个或多个风险关联，每条关系独立保存“是否必要”和验证结论。

待确认阶段替换评估来源时，两类来源关系在同一事务内同步。新增来源必须对应可升级的不符合项；
移除来源后，无其他风险关联的不符合项恢复待处置，仍有关联的保留原状态。确认风险前校验评估来源与
不符合项关系一致；历史不一致记录需先核对纠正，系统不会自动重写既有审核结论。

```text
评估项目 ── 评估单元 ── 不符合项 ─┐
   │          │                    ├─ 风险 ── 受影响资产
   │          └──── risk_sources ──┘   │
   └── taskId（仅评估风险）             └─ risk_action_links ── 整改行动

人工来源字段 ── 人工风险（taskId=null）── 独立审核人
```

## 生命周期与职责

风险统一经历“待确认 → 已识别 → 整改中 → 待验证 → 已关闭”，也可在已识别/整改中选择正式接受并
设置复查日期。待确认阶段可以编辑和删除；确认后来源只读。整改行动只能关联已确认且未关闭/取消的
风险，全部必要关系验证通过后风险才能关闭。

评估风险由项目审计员确认/验证；人工风险由指定审核人确认/验证。具备全量数据范围的管理员可执行
兜底操作，整改负责人始终不能验证自己的整改。人工风险不进入评估项目页，也不参与项目关闭门禁。

## 删除与回滚

评估项目到风险的外键使用 `RESTRICT`，避免删除项目时级联丢失风险。删除待确认风险前，审计日志
保存人工来源字段或评估项目/评估单元摘要。026 回滚会先检查独立风险；存在 `taskId=null`、人工模式
或非合规评估来源的风险时拒绝回滚。
