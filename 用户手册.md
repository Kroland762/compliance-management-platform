# 合规管理平台 — 用户手册

---

## 一、系统概述

合规管理平台包含三大业务模块：

- **合规评估**：合规标准 → 评估范围 → 表格式填写 → 多审计员复核 → 不符合项 → 整改/风险 → 评估关闭
- **产品合规**：产品 → 产品版本 → 问卷/权限/信息类型/ROPA → 合规复核 → 版本档案
- **账户审计**：多数据源接入 → 规则引擎自动检测 → 问题追踪

支持“全局登录身份 + 租户成员”体系：同一账号可加入多个租户，并在每个租户拥有独立角色和部门。

---

## 二、角色与权限

系统内置 3 个锁定角色，管理员可复制系统角色后创建自定义角色（系统设置 → 角色管理）。

| 角色 | 典型权限 |
|------|---------|
| 管理员 | 全部权限，可管理用户、角色、安全设置 |
| 审计员 | 查看被分配项目，认领和复核单元，处置不符合项、风险及整改验证；不查看合规标准、资产台账、资质台账 |
| 普通用户 | 填写本人评估单元、处理本人整改事项、查看通知和个人设置 |

---

## 三、登录与安全

### 3.1 登录

1. 打开系统页面
2. 输入用户名、密码
3. 输入图形验证码（点击图片可刷新）
4. 点击「登录」

只有一个有效租户成员关系时会自动进入；有多个租户时先进入“选择租户”页。新本地成员或
首位租户管理员使用一次性临时密码，首次登录只能修改密码或退出，改密后需重新登录。

### 3.2 安全策略

| 策略 | 说明 |
|------|------|
| 图形验证码 | 每次登录必须输入，5 分钟有效 |
| 密码复杂度 | 至少 8 位，须含大写、小写、数字、特殊字符 |
| 登录锁定 | 连续失败 N 次后锁定 M 分钟（管理员可配置） |
| 空闲登出 | 无操作超过设定时间后自动退出（管理员可配置） |

### 3.3 修改密码

点击右上角头像 → 设置 → 修改密码，输入当前密码和新密码即可。

### 3.4 修改安全策略（管理员）

点击右上角头像 → 设置，可调整：
- 最大登录失败次数（默认 5 次）
- 锁定时长（默认 15 分钟）
- 空闲自动登出时间（默认 3 小时）

---

## 四、合规评估模块

### 4.1 工作流

```
管理员选择标准和资产范围、配置审计员池并发布表格式评估 →
普通成员填写和提交 → 审计员认领并复核 → 自动生成不符合项 →
直接整改或升级风险 → 整改验证/风险接受或关闭 → 管理员关闭评估
```

### 4.2 管理员操作

**创建或邀请成员**
1. 系统设置 → 成员管理
2. “新建本地成员”：填写用户名、姓名，选择一个或多个角色、主部门和兼职部门
3. 保存后立即安全记录一次性临时密码；关闭弹窗后不能再次查看
4. “邀请已有身份”：输入现有用户名，配置该租户的角色和部门，将一次性邀请链接发送给本人

**导入合规标准**
1. 基础数据 → 合规标准
2. 点击「导入 CSV 模板」，选择文件后先预览列和前 5 行
3. CSV 必填列：序号、控制域名、控制点；所有其他列也会完整导入
4. 设置标准系列标识、版本和稳定控制项标识列；跨版本沿用同一系列标识
5. 勾选评估表需要展示的模板列。导入后可在模板详情表头使用左右按钮调整全部业务列和系统列；保存后的布局自动用于后续发布的项目，已有项目可由管理员在评估表中点击“同步模板列配置”更新布局

**创建评估项目**
1. 合规评估 → 评估项目 → 新建评估项目
2. 选择标准版本，填写项目名称、归属部门和评估周期，并配置一名或多名审计员
3. 选择资产范围。归属部门和默认责任人会自动带入所有评估行
4. 预览后发布。系统按控制项生成评估表，每行默认关联本次选择的全部资产，并保存标准列、控制项和资产摘要快照

项目主状态统一为：准备中 → 待开始 → 进行中 → 待复核 → 待闭环 → 已关闭。退回和提交属于评估行状态，不再单独拆分项目状态；已取消作为异常终态保留。

### 4.3 审计员操作

**认领和复核评估行**
1. 进入「我的待办 → 待复核」，可看到当前账号所在审计员池的共享待复核行
2. 点击「认领」。同一行只能被一名审计员认领；冲突时刷新后选择其他行
3. 认领人可以释放；管理员可以在项目中强制转派；填写人不能认领或复核自己填写的行
4. 逐项选择符合、部分符合、不符合或不适用；部分符合/不符合必须填写不符合事实与严重度
3. 点击历史证据或当前证据文件名，可直接预览图片、CSV 和 PDF
5. 需要补充时可退回填写人，认领会自动清除；重新提交后回到共享复核池

### 4.4 用户操作

1. 登录后进入「我的待办 → 待填写」
2. 进入项目评估表，直接在“现状说明”列逐行填写；停止输入后自动保存
3. 每行默认关联项目创建时选择的资产。提交前可修改关联资产，或将部分资产拆分成新行；拆分复制回答草稿但不复制证据
4. 使用表格上方的关键词、资产、控制域、流程状态、符合性结论和“仅看待我处理”进行快捷筛选；点击各列表头的筛选图标可进一步按该列筛选。多列条件同时满足，同一列多选值满足任意一个即可
5. 已生效条件会显示在表格上方并保存在当前页面地址中，可单独移除或点击“清除全部”；分页区同时显示筛选后数量和项目全部数量。现状说明只支持“已填写/未填写”，不会搜索加密回答正文
6. 点击“历史回答与证据”查看同一标准系列、同一稳定控制项且资产重叠的已复核历史回答和证据文件；首次评估或无匹配记录时显示暂无匹配的历史回答或证据
7. 点击历史证据或当前证据文件名，可直接预览图片、CSV 和 PDF；其他格式仍可下载
8. 可单行提交，也可勾选多行批量提交；批量校验失败时所有选中行均不提交
5. 被退回后重新填写

### 4.5 不符合项、风险与整改

部分符合或不符合的单元复核完成后会自动生成一条唯一不符合项。一般问题选择「直接整改」，
创建或关联整改行动；所有必要整改关联验证通过后，不符合项自动解决。需要统一评估影响的问题，
可勾选同一项目的一条或多条不符合项升级为风险。风险完成整改后关闭，或填写接受理由和复查
日期后正式接受，关联不符合项才会解决。

项目全部评估单元复核后状态为「待闭环」。仍有未解决不符合项时，后端会阻止关闭；全部
解决后管理员在项目详情关闭评估。项目详情包含概览、评估范围、评估单元、不符合项、风险与
整改、操作记录六个页签。

---

## 五、产品合规模块

### 5.1 配置产品类型与问卷

1. 进入「产品合规 → 配置管理」，创建产品类型。
2. 创建问卷模板版本，填写问卷系列标识、版本和具有稳定标识的问题；问卷支持是/否、单选、多选、文本、数字和日期。
3. 启用后的问卷内容不可覆盖。如需修改问题，创建同一系列的新版本。
4. 在产品类型中配置自动适用的一个或多个启用问卷。

### 5.2 创建产品和产品版本

1. 进入「产品合规 → 产品台账」，填写产品编码、名称、默认类型、负责人和归属部门。
2. 打开产品详情并创建版本，填写版本号、适用平台、使用范围、上线日期和变更声明。
3. 系统按当前产品类型规则生成问卷组合。存在上一版本已确认档案时，权限、信息类型和 ROPA 会复制到新草稿；稳定题目标识相同的答案会继承。
4. 继承的合规结论只作为参考，新档案的正式结论仍为“未评估”。

### 5.3 维护合规档案

档案页面包含概览、产品问卷、权限/信息与 ROPA、复核记录：

- 概览中填写拟定合规结论并检查版本变更和继承差异。
- 产品问卷可逐题填写；如需改变自动匹配结果，选择“调整适用问卷”并填写原因。
- 平台权限记录 Android、iOS、Web 等平台需要的权限、用途、必要性和关联信息类型。
- 信息类型记录产品处理的个人数据类别、数据主体、来源、敏感性和必要性。
- ROPA 按处理活动记录目的、法律依据、数据主体、接收方、跨境、保存期限、安全措施和责任主体，并关联一个或多个信息类型。
- 未申请平台权限或不处理个人数据时，必须使用页面上的明确声明，不能只保留空列表。

### 5.4 提交、复核和修订

1. 产品负责人完成必填问卷和合规清单后提交复核。
2. 合规人员进入「产品合规 → 待复核」，可以退回并填写原因，或确认最终合规结论；提交人不能复核本人提交的档案。
3. 流程状态为草稿、待复核、已退回、已确认、已取代；合规结论单独为未评估、符合、有条件符合、不符合。
4. 已确认档案锁定。发现错误时选择“创建修订”，完成新修订复核后，旧档案标记为已取代。
5. 版本计划上线日期已到但档案未确认时，系统显示提醒，但不会阻断产品上线。

---

## 六、账户审计模块

### 6.1 概览

顶部统计卡片（数据源数、账户总数、问题总数、高风险数、待处理数），下方风险分布饼图、问题排行柱状图、趋势图。

### 6.2 数据源

**接入数据源**
1. 右侧菜单 → 账户审计 → 数据源
2. 点击「添加数据源」，选择数据库直连或 CSV 上传
3. 配置字段映射（将源字段映射到标准字段）
4. 点击「同步」导入账户数据

数据分 3 层生命周期：HOT（当前）→ WARM（上一批次）→ COLD（将清理）。

**预览数据**：数据源详情页可预览前 10 条账户。

**账户变更统计**：对比 HOT 和 WARM，显示新增/减少/存量。

### 6.3 规则管理

**内置规则**（不可删除，可配置参数）：
- 长期未登录（默认 180 天）
- 未启用 MFA
- 高权限账户未启用 MFA
- 异常创建时间

**自定义规则**：
1. 规则管理页面 → 创建规则
2. 填写名称、严重度、描述
3. 配置检测条件：选择字段、运算符、值
4. 支持 AND/OR 逻辑切换和嵌套条件组
5. 保存后规则即生效

**规则引擎运算符**：等于、不等于、包含、大于、小于、距今超过N天、未开启/为空 等 10+ 种。

### 6.4 审计任务

1. 任务管理 → 创建任务
2. 选择数据源、关联规则、设置调度方式
3. 调度方式：手动 / 每天 / 每周 / 每月 / Cron 表达式
4. 点击「执行」手动触发，或在调度时间自动执行

**执行流程**：同步数据 → 字段映射 → 规则匹配 → 去重 & 保存问题。

**执行记录**：每次执行生成一条历史记录，显示处理账户数、发现问题数。

### 6.5 问题管理

审计任务执行后自动生成问题列表。

| 功能 | 说明 |
|------|------|
| 筛选 | 按严重度、状态、规则、日期范围筛选 |
| 详情 | 点击问题查看详情（账户信息、触发规则、严重度） |
| 状态更新 | 待处理 → 处理中 → 已解决 / 误报 / 忽略 |
| 批量操作 | 勾选多条 → 一键标记状态 |
| 导出 | CSV / JSON 格式导出 |

**智能去重**：同一 `账户 + 规则` 跨任务不会重复建问题。问题消失时自动关闭为「自动修复」。

---

## 七、角色与组织治理（管理员）

1. 系统设置 → 角色管理
2. 权限按“资源 × 动作 × 数据范围”配置
3. 点击「新建角色」创建自定义角色
4. 同一成员可绑定多个角色；权限取并集，数据范围选择最宽一级
5. 在成员管理中分配角色，在组织树中维护部门资料和负责人

**系统内置角色不可编辑或删除**，只能复制为自定义角色后修改。任何操作都不能停用自己、
移除最后一个有效管理员，或删除仍被成员使用的角色。

每个有效成员必须有且只有一个主部门，可以有多个兼职部门。部门编码租户内唯一且创建后
不可修改，层级最多十级并禁止循环；`ROOT` 根部门编码固定。任务和资质保存创建时的稳定
归属部门，人员调岗不会自动改变历史对象归属。删除部门会永久移除该节点且不可恢复，仅允许
删除无子部门、无成员、未被任何业务数据引用的非根部门；不满足条件时系统会提示阻止原因。

### 7.1 资产台账

资产台账支持新增和编辑资产，资产编码创建后不可修改。点击“归档”只会把资产状态改为
“已归档”，数据仍保留在同一资产台账中；使用列表上方的“已归档”或“全部”筛选即可查看。
已归档资产不会进入新的评估范围，也不能直接编辑；点击“恢复”后可重新使用和编辑。

---

## 八、工作台

登录后的首页，包含：
- 统计卡片：评估项目、已完成（待闭环及已关闭）、合规标准、我的待办、不符合项、风险和整改
- 评估项目分布饼图
- 风险级别分布饼图

---

## 九、通知系统

右上角铃铛图标，显示未读通知数：
- 新任务分配通知
- 任务提交通知
- 任务退回通知

点击可标记已读。

---

## 十、常见问题

**Q: 为什么看不到某些菜单？**
A: 你的角色没有对应权限。联系管理员调整角色权限。

**Q: 忘记密码/账户被锁定？**
A: 联系管理员在用户管理页面重置或解锁。

**Q: 评估为什么不能关闭？**
A: 关闭是后端门禁。请确认全部评估单元已经复核，且所有不符合项已经完成整改验证，或其关联风险已经关闭/正式接受。

**Q: 提交后还能修改吗？**
A: 只有被退回后才能重新编辑。

**Q: 账户审计问题列表为空？**
A: 需要先创建数据源（导入账户数据）→ 创建并执行审计任务。

**Q: 自动登出了？**
A: 超过空闲时间无操作，重新登录即可。超时时间可在设置页面调整。

---

## 十一、租户上下文

全局管理员和多租户成员登录后通过租户选择页进入；进入后顶部切换器可切换其他允许的租户。
选择仅保存在当前浏览器会话的 `sessionStorage` 中，退出、租户停用或成员停用后自动清除。
未选择租户时，全局管理员只能使用平台管理中的租户管理功能，普通成员不能读取业务数据。

租户态令牌包含 userId、memberId、tenantId 和会话版本，但权限、角色、部门范围每次请求
均从数据库重新加载。`X-Tenant-ID` 必须与令牌租户一致；伪造会被拒绝并审计。无权访问的
任务、风险、资质、题目、证据和导出统一显示为“不存在”，避免泄露对象存在性。
