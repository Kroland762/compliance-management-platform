# 合规管理平台部署与升级指南

## 1. 支持基线

- Node.js 22.x。
- PostgreSQL 14 或更高版本。
- Docker Engine + Docker Compose v2（单机生产部署）。
- `public` 仅存控制面：用户、租户、角色模板、迁移记录、控制面审计和调度租约。
- 每个租户的角色、组织、任务、风险、资质、证据和账户审计数据位于独立 schema。
- 账户审计可接入 PostgreSQL、MySQL、SQL Server、Oracle、SQLite 只读数据库数据源和 CSV；
  生产环境中的网络数据库数据源必须启用 TLS，SQLite 文件必须位于后端容器可访问的持久化路径。

生产环境必须配置正式 `DOMAIN`，以及强随机 `JWT_SECRET`、`ENCRYPTION_KEY`、
`METRICS_TOKEN` 和 `BACKUP_ENCRYPTION_KEY`。备份密钥应保存在仓库外的受保护文件中。
日志不得采集密码、Token、连接密码、问卷原文或文件内容。
资质状态、筛选和统计统一使用 `BUSINESS_TIME_ZONE` 的日历边界，默认
`Asia/Shanghai`；所有实例必须配置为同一有效 IANA 时区。

## 2. 全新安装

单台云服务器、域名、证书、Nginx 和防火墙的完整步骤以《上云部署指南》为准。本节仅描述
应用和数据库初始化顺序。

Docker Compose 启动前先生成独立密钥，不得使用示例值：

```bash
cp .env.docker.example .env
# 编辑 .env，设置正式 DOMAIN、证书邮箱和独立强随机密钥
chmod 600 .env
./deploy/cloud-preflight.sh .env
docker compose --env-file .env build
docker compose --env-file .env run --rm migrate
# 仅全新安装：交互式加载初始管理员密码后执行 seed:prod
docker compose --env-file .env up -d
curl --fail http://127.0.0.1:3001/api/health/ready
```

Compose 中 PostgreSQL、后端和前端端口均只绑定到 `127.0.0.1`，不对局域网或公网暴露。
主机 Nginx 是唯一公网入口，且后端 `TRUST_PROXY_HOPS=1` 只对应这一层代理。增加云负载均衡
或 CDN 前必须重新设计真实 IP 信任边界。

```bash
cd backend
npm ci
npm run migrate:up
SEED_ADMIN_USERNAME=admin SEED_ADMIN_PASSWORD='your-strong-password' npm run seed
npm run migrate:status
npm run build

cd ../frontend
npm ci
npm test
npm run build
```

固定顺序是“配置密钥 → 构建 → 版本化迁移 → 显式 seed → 启动 → readiness”。
`backend` 服务依赖一次性 `migrate` 服务成功完成，但不会自动执行 seed，避免重启时重复初始化。
禁止以 `sequelize.sync({ alter: true })` 代替生产迁移；它没有迁移 ID、checksum、回滚门禁或
多租户 schema 编排能力，只允许测试用例在独立临时 schema 中建模。

使用控制面管理员创建租户。创建流程会运行全部租户迁移并从全局角色模板复制租户角色，
不会复制 `public.users` 或旧 `public.roles`。

## 3. 存量升级

1. 停止写流量并执行加密备份。
2. 如果旧 Docker 数据卷曾使用固定数据库密码，先在受控终端中运行
   `psql` 的 `\password admin`，交互式设置与新 `.env` 一致的强密码；不要把密码
   写入命令行历史。
3. 在临时 PostgreSQL 恢复备份并运行 `npm run migrate:up`。
4. 执行 `npm run migrate:legacy-public` 查看 public 业务表和关联数量。
5. 执行 `npm run migrate:membership-plan > membership-plan.json`，检查用户、角色、主部门以及
   任务、资质和非人工模式无任务风险的映射缺口；`creationMode=manual` 且 `taskId=null` 是合法数据。
6. 对零部门、多部门、无效角色和历史文本部门编写显式 mapping；先评审，再运行
   `npm run migrate:membership-plan -- --apply --mapping=/absolute/path/membership-map.json`。
7. 明确 public 目标租户映射后运行 `npm run migrate:legacy-public -- --apply --tenant=<id>`。
8. 对旧证据先运行 `npm run migrate:legacy-files -- --tenant=<id>`，核对清单后增加 `--apply`。
9. 验证数量、哈希、跨租户 404、分页统计和所有角色 UAT 后再切换生产。
10. 认证会话迁移不接受无 `tokenUse` 和 `sessionId` 的历史 JWT；发布后通知所有
    用户重新登录一次。之后刷新令牌为一次性轮换，登出会立即撤销服务端会话。

迁移工具不会猜测租户归属，也不会删除 public 原数据或旧文件。验收结束后如需清理，
应另开变更单并保留可验证备份。

成员清单会探测实际字段，兼容 004 阶段历史风险标题及 026 前尚无 `creationMode` 的 schema。
旧 public 风险导入到 026 后的租户时，缺失的创建方式补为 `import`，发现来源补为
`compliance_assessment`；创建人依次取最早不符合项关联、最早评估来源关联、负责人。
无法确定有效全局用户时，导入会列出风险 ID 并整批回滚。应修复明确的数据映射后重试，
不得禁用非空约束；重复导入仍核对原有共有字段的数量和哈希。

成员治理迁移会在清理旧字段前强制验证：每个 active 成员恰好一个主部门、至少一个角色，
全部任务和资质具有稳定归属部门；评估/不符合项风险必须关联任务，人工风险必须不关联任务并指定
独立审核人。`public.user_identity_migration_snapshots`
保留旧 tenant/role/department 值，用于受保护的单步回滚。缺少 mapping 时 `--apply` 必须失败。

### 3.1 独立风险迁移（026）

升级前对 PostgreSQL 全库（包含所有 tenant schema）执行可验证备份，并确认
`npm run migrate:status` 没有 checksum 异常。`026_tenant_independent_risks` 会回填历史风险为
`evaluation` 或 `finding_escalation`，发现来源统一为 `compliance_assessment`，创建人优先取现有
关系记录，最后回退负责人；随后放宽 `taskId`、增加来源/审核字段及一致性约束。

迁移后至少验证：项目风险精确过滤、人工风险创建/确认、整改提交/审核人验证、待办和通知、风险
趋势、Excel 来源工作表、审计日志。若业务验收失败但已产生独立风险，不得执行 down；应关闭
`/risks/new` 入口、回退应用代码到兼容版本并保留数据库，或从已验证备份恢复到临时环境制定数据
处置方案。只有没有任何独立风险时，026 down 才允许恢复旧的 `taskId NOT NULL` 约束。

## 4. 备份与恢复

脚本默认 dry-run。Docker 单机部署统一使用 Compose 的 `backup-tools` profile；它会只读挂载
uploads volume，并从 `.env` 中 `BACKUP_KEY_FILE` 指向的受保护文件加载密钥：

```bash
docker compose --env-file .env --profile ops run --rm backup-tools backup.sh
docker compose --env-file .env --profile ops run --rm backup-tools backup.sh --apply
```

恢复必须使用空的临时数据库和空目录，并显式二次确认数据库名：

```bash
docker compose --env-file .env --profile ops run --rm \
  -e BACKUP_FILE=/data/backups/<backup-file>.tar.enc \
  -e RESTORE_DB_NAME=compliance_restore \
  -e RESTORE_CONFIRM_DB=compliance_restore \
  -e RESTORE_UPLOAD_DIR=/data/restore-uploads \
  backup-tools restore.sh --apply
```

恢复脚本验证数据库转储和上传归档的 SHA-256 manifest。验证完成前保留原备份与上传目录。
数据库、uploads 和历史 `ENCRYPTION_KEYRING` 缺一不可；备份文件应异地保存，密钥环应与数据
备份分开保管。生产恢复必须先在空数据库和空目录演练，不直接覆盖在线 volume。

## 5. 健康、指标与日志

- `GET /api/health/live`：进程、内存和事件循环。
- `GET /api/health/ready`：数据库、迁移校验和和调度器。
- `GET /api/health`：兼容入口，等价于 ready。
- `GET /api/internal/metrics`：Prometheus 文本；生产必须发送
  `Authorization: Bearer <METRICS_TOKEN>` 或 `X-Metrics-Token`。

主机 Nginx 模板默认阻止互联网访问 health 和 metrics；运维与监控从服务器回环地址访问。

日志是 Winston JSON，包含 requestId、tenantId、userId、路由、结果和耗时。

## 6. 故障排查

- `TENANT_CONTEXT_REQUIRED`：全局管理员尚未选择租户。
- `TENANT_INACTIVE`：租户已停用；前端会清除 sessionStorage 中的选择。
- `MEMBERSHIP_INACTIVE`：当前租户成员已停用或离职；旧租户态令牌不可继续使用。
- `PASSWORD_CHANGE_REQUIRED`：一次性临时密码尚未修改，只允许改密和退出。
- 迁移校验和不一致：停止发布，不要修改已发布迁移；恢复原迁移文件或新增后续迁移。
- ready 返回 503：检查数据库、`schema_migrations` 和调度器状态。
- 数据源连接被拒绝：确认方言为 PostgreSQL、MySQL、SQL Server、Oracle 或 SQLite；网络数据库启用
  TLS，主机未命中内网/元数据地址阻断，且表名、schema 和字段白名单使用普通数据库标识符。
- 资质日期边界异常：确认所有实例的 `BUSINESS_TIME_ZONE` 一致；修改后需重启后端。
- 调度执行失败：检查 `public.task_schedules` 的租约/心跳及租户
  `account_task_executions` 的 idempotencyKey、attempt、nextRetryAt。
- 调度任务超时：检查 `SCHEDULE_TASK_TIMEOUT_MS`（默认 600000ms）以及执行记录的阶段和心跳；
  超时会明确标记计划失败并进入指数退避。

## 7. 已知限制

- 不部署 Redis、MinIO、Prometheus Server 或 Grafana。
- S3-compatible 仅保留接口和配置契约，本版本使用本地 tenant-scoped storage key。
- 当前生产拓扑为单后端实例；验证码和限流仍为进程内状态，不支持直接横向扩容。
- CSV 可用于合规模板导入和账户审计数据源导入；账户审计数据库直连只接受只读表和字段白名单，
  不接受任意 SQL。
- 通用 cron 表达式由 node-cron 解析；跨时区统一按 `Asia/Shanghai` 触发。
- 发布门禁以执行当时的 `npm audit --omit=dev` 结果为准；文档不保存可能过期的具体公告清单。
  若审计源不可访问，应将其记录为外部验证限制，不得把网络失败解释为“无漏洞”。
