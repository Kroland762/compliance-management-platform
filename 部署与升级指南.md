# 合规管理平台部署与升级指南

## 1. 支持基线

- Node.js 22.x。
- PostgreSQL 14 或更高版本。
- `public` 仅存控制面：用户、租户、角色模板、迁移记录、控制面审计和调度租约。
- 每个租户的角色、组织、任务、风险、资质、证据和账户审计数据位于独立 schema。

生产环境必须配置强随机 `JWT_SECRET`、`ENCRYPTION_KEY`、`METRICS_TOKEN` 和
`BACKUP_ENCRYPTION_KEY`。日志不得采集密码、Token、连接密码、问卷原文或文件内容。
资质状态、筛选和统计统一使用 `BUSINESS_TIME_ZONE` 的日历边界，默认
`Asia/Shanghai`；所有实例必须配置为同一有效 IANA 时区。

## 2. 全新安装

```bash
cd backend
npm ci
npm run migrate:up
SEED_ADMIN_USERNAME=admin SEED_ADMIN_PASSWORD='your-strong-password' npm run seed
npm run migrate:status
npm run build

cd ../frontend
npm ci
npm test
npm run build
```

使用控制面管理员创建租户。创建流程会运行全部租户迁移并从全局角色模板复制租户角色，
不会复制 `public.users` 或旧 `public.roles`。

## 3. 存量升级

1. 停止写流量并执行加密备份。
2. 在临时 PostgreSQL 恢复备份并运行 `npm run migrate:up`。
3. 执行 `npm run migrate:legacy-public` 查看 public 业务表和关联数量。
4. 执行 `npm run migrate:membership-plan > membership-plan.json`，检查用户、角色、主部门以及
   任务、资质和无任务风险的映射缺口。
5. 对零部门、多部门、无效角色和历史文本部门编写显式 mapping；先评审，再运行
   `npm run migrate:membership-plan -- --apply --mapping=/absolute/path/membership-map.json`。
6. 明确 public 目标租户映射后运行 `npm run migrate:legacy-public -- --apply --tenant=<id>`。
7. 对旧证据先运行 `npm run migrate:legacy-files -- --tenant=<id>`，核对清单后增加 `--apply`。
8. 验证数量、哈希、跨租户 404、分页统计和所有角色 UAT 后再切换生产。

迁移工具不会猜测租户归属，也不会删除 public 原数据或旧文件。验收结束后如需清理，
应另开变更单并保留可验证备份。

成员治理迁移会在清理旧字段前强制验证：每个 active 成员恰好一个主部门、至少一个角色，
全部任务和资质具有稳定归属部门、全部风险关联任务。`public.user_identity_migration_snapshots`
保留旧 tenant/role/department 值，用于受保护的单步回滚。缺少 mapping 时 `--apply` 必须失败。

## 4. 备份与恢复

脚本默认 dry-run：

```bash
cd backend
DB_NAME=... DB_USER=... BACKUP_ENCRYPTION_KEY=... ./scripts/backup.sh
DB_NAME=... DB_USER=... BACKUP_ENCRYPTION_KEY=... ./scripts/backup.sh --apply
```

恢复必须使用空的临时数据库和空目录，并显式二次确认数据库名：

```bash
BACKUP_FILE=... RESTORE_DB_NAME=compliance_restore \
RESTORE_CONFIRM_DB=compliance_restore RESTORE_UPLOAD_DIR=/tmp/compliance-uploads-restore \
DB_USER=... BACKUP_ENCRYPTION_KEY=... ./scripts/restore.sh --apply
```

恢复脚本验证数据库转储和上传归档的 SHA-256 manifest。验证完成前保留原备份与上传目录。

## 5. 健康、指标与日志

- `GET /api/health/live`：进程、内存和事件循环。
- `GET /api/health/ready`：数据库、迁移校验和和调度器。
- `GET /api/health`：兼容入口，等价于 ready。
- `GET /api/internal/metrics`：Prometheus 文本；生产必须发送
  `Authorization: Bearer <METRICS_TOKEN>` 或 `X-Metrics-Token`。

日志是 Winston JSON，包含 requestId、tenantId、userId、路由、结果和耗时。

## 6. 故障排查

- `TENANT_CONTEXT_REQUIRED`：全局管理员尚未选择租户。
- `TENANT_INACTIVE`：租户已停用；前端会清除 sessionStorage 中的选择。
- `MEMBERSHIP_INACTIVE`：当前租户成员已停用或离职；旧租户态令牌不可继续使用。
- `PASSWORD_CHANGE_REQUIRED`：一次性临时密码尚未修改，只允许改密和退出。
- 迁移校验和不一致：停止发布，不要修改已发布迁移；恢复原迁移文件或新增后续迁移。
- ready 返回 503：检查数据库、`schema_migrations` 和调度器状态。
- 数据源连接被拒绝：确认只使用 PostgreSQL、TLS、白名单字段和允许的网络地址。
- 资质日期边界异常：确认所有实例的 `BUSINESS_TIME_ZONE` 一致；修改后需重启后端。
- 调度执行失败：检查 `public.task_schedules` 的租约/心跳及租户
  `account_task_executions` 的 idempotencyKey、attempt、nextRetryAt。
- 调度任务超时：检查 `SCHEDULE_TASK_TIMEOUT_MS`（默认 600000ms）以及执行记录的阶段和心跳；
  超时会明确标记计划失败并进入指数退避。

## 7. 已知限制

- 不部署 Redis、MinIO、Prometheus Server 或 Grafana。
- S3-compatible 仅保留接口和配置契约，本版本使用本地 tenant-scoped storage key。
- CSV 仍可用于合规模板导入；账户审计数据源不再接受 CSV 或任意 SQL。
- 通用 cron 表达式由 node-cron 解析；跨时区统一按 `Asia/Shanghai` 触发。
- 当前 `npm audit` 仍报告 React Router 的 RSC 模式 CSRF 公告；本项目是纯客户端 SPA，
  未启用 RSC、SSR、Server Action 或 React Router 服务端处理器。升级前持续跟踪上游修复。
- ExcelJS 4.4 的 `archiver`/`uuid` 传递依赖仍会触发生产依赖审计告警；平台导出使用固定
  文件结构且不把外部输入作为归档路径，但仍应在上游发布兼容修复后尽快升级或替换。
- Sequelize 6.37 的旧 `uuid` 传递依赖还会触发缓冲区边界公告；本项目只使用 ORM 的 UUIDv4
  生成路径，未调用受影响的 v3/v5/v6 外部缓冲区接口，待 Sequelize 发布兼容依赖后升级。
