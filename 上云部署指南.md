# 合规管理平台单机上云部署指南

本文是“单台云服务器 + Docker Compose + 主机 Nginx + HTTPS 域名”的唯一上云入口。
数据库升级、存量数据迁移和回滚细节另见《部署与升级指南》。不要使用 PM2、
`npm install` 或 `sequelize.sync({ alter: true })` 部署本项目。

## 1. 目标架构

```text
Internet
   │ 80/443
   ▼
Host Nginx + Let's Encrypt
   ├── /      -> 127.0.0.1:8080 -> frontend container
   └── /api/  -> 127.0.0.1:3001 -> backend container
                                      │
                                      ├── PostgreSQL container
                                      └── uploads named volume
```

PostgreSQL、后端和前端容器端口都只绑定 `127.0.0.1`。安全组或主机防火墙只对公网开放
80/443；SSH 仅允许受控来源。不要对公网开放 3001、5432 或 8080。
账户审计如需连接外部数据库数据源，只允许由后端容器发起出站连接；生产环境的网络数据库数据源
必须启用 TLS，并继续受表名、字段白名单和网络地址阻断规则约束。

## 2. 前置条件

- 64 位 Linux 云服务器，建议至少 2 核、4 GiB 内存和足够的持久磁盘。
- 已安装并启动 Docker Engine，以及 Docker Compose v2 插件。
- 已安装 Nginx、Certbot 和 PostgreSQL 客户端工具。
- 域名 A/AAAA 记录已经指向服务器公网地址，80/443 可以从互联网访问。
- 发布代码必须来自已提交且 CI 通过的版本，不直接复制未提交工作树。

Docker Engine 和 Compose 应按
[Docker Engine 官方安装文档](https://docs.docker.com/engine/install/)和
[Compose 插件官方文档](https://docs.docker.com/compose/install/linux/)安装；不要在生产机使用
convenience script。证书操作以 [Certbot 官方文档](https://eff-certbot.readthedocs.io/en/stable/using.html)
为准。
安装后确认：

```bash
docker version
docker compose version
sudo systemctl is-active docker
nginx -v
certbot --version
pg_dump --version
```

## 3. 获取代码与配置

```bash
sudo install -d -m 0750 -o "$USER" -g "$USER" /opt/compliance-platform
git clone <repository-url> /opt/compliance-platform/app
cd /opt/compliance-platform/app
cp .env.docker.example .env
chmod 600 .env
```

编辑 `.env`：

- `DOMAIN` 只填写域名，不带 `https://` 或路径。
- `LETSENCRYPT_EMAIL` 填写证书到期通知邮箱。
- `DB_PASSWORD`、`JWT_SECRET`、`ENCRYPTION_KEY` 和 `METRICS_TOKEN` 必须分别生成，禁止复用。
- `BACKUP_ENCRYPTION_KEY` 不应长期写在项目 `.env`；建议保存在仓库外的 0600 文件中，
  运行备份时临时加载。
- `BACKEND_HOST_PORT`、`FRONTEND_HOST_PORT` 和 `DB_HOST_PORT` 默认值均只绑定回环地址，
  通常无需修改。

可用 OpenSSL 生成独立随机值：

```bash
openssl rand -hex 32
```

创建备份密钥和恢复演练目录。以下命令会生成新的备份密钥，只能在首次初始化时执行；已有备份时
必须继续使用原密钥：

```bash
sudo install -d -m 0750 /etc/compliance
sudo install -d -m 0750 /var/backups/compliance-platform
sudo install -d -m 0750 /var/lib/compliance-platform/restore-uploads
openssl rand -hex 32 | sudo tee /etc/compliance/backup.key >/dev/null
sudo chmod 600 /etc/compliance/backup.key
```

配置完成后运行预检：

```bash
./deploy/cloud-preflight.sh .env
```

预检只校验配置完整性和 Compose 结构，不会启动容器或修改数据库。

## 4. 首次构建、迁移与初始化

以下操作会创建数据库和 uploads Docker volume，并写入初始 schema。全新安装才执行 seed；
如果是存量迁移，先按《部署与升级指南》恢复备份，不要重新创建管理员。

```bash
docker compose --env-file .env build
docker compose --env-file .env run --rm migrate

read -rsp 'Initial admin password: ' SEED_ADMIN_PASSWORD
echo
export SEED_ADMIN_PASSWORD
docker compose --env-file .env run --rm \
  -e SEED_ADMIN_USERNAME=admin \
  -e SEED_ADMIN_PASSWORD \
  backend npm run seed:prod
unset SEED_ADMIN_PASSWORD

docker compose --env-file .env up -d
docker compose --env-file .env ps
curl --fail http://127.0.0.1:3001/api/health/ready
curl --fail http://127.0.0.1:8080/
```

`migrate` 服务可以重复执行，但 seed 必须显式执行。生产环境禁止使用
`sequelize.sync({ alter: true })` 代替版本化迁移。

## 5. 申请证书并安装 Nginx

在首次安装最终 HTTPS 配置前，确保证书目录已经存在。新服务器可临时停止 Nginx，使用
Certbot standalone 模式申请证书：

```bash
DOMAIN='你的正式域名'
LETSENCRYPT_EMAIL='你的证书通知邮箱'

sudo systemctl stop nginx
sudo certbot certonly --standalone \
  --non-interactive --agree-tos \
  --email "$LETSENCRYPT_EMAIL" \
  -d "$DOMAIN"
```

从仓库模板生成站点配置。模板中的端口必须与 `.env` 保持一致：

```bash
./deploy/render-nginx.sh .env /tmp/compliance-nginx.conf
sudo install -m 0644 /tmp/compliance-nginx.conf /etc/nginx/sites-available/compliance

sudo ln -sfn /etc/nginx/sites-available/compliance /etc/nginx/sites-enabled/compliance
sudo nginx -t
sudo systemctl enable --now nginx
sudo certbot renew --dry-run
```

最终 Nginx 配置会：

- 将 HTTP 永久重定向至 HTTPS；
- 只把 `/api/` 转发给回环地址上的后端；
- 设置 HSTS、CSP、`nosniff` 和 Referrer Policy；
- 阻止互联网直接读取 readiness 和 Prometheus 指标；
- 传递一层受控 `X-Forwarded-*` 头，与后端 `TRUST_PROXY_HOPS=1` 对应。

如果服务器前面还有云负载均衡或 CDN，代理层数和真实客户端 IP 规则会变化。此时不要直接增加
`TRUST_PROXY_HOPS`，应先明确整条代理链和受信来源，再修改配置并验证伪造头无法绕过限流。

## 6. 上线验收

```bash
# HTTP 必须跳转 HTTPS
curl -I "http://$DOMAIN"

# 首页和登录接口必须从公网域名访问成功
curl --fail "https://$DOMAIN/"
curl --fail "https://$DOMAIN/api/auth/captcha"

# 内部端口只能监听 127.0.0.1
ss -lntp | grep -E ':(3001|5432|8080)\b'

# 容器和依赖必须健康
docker compose --env-file .env ps
curl --fail http://127.0.0.1:3001/api/health/ready
```

还必须由浏览器完成以下业务验收：

1. 使用正式域名登录，刷新页面后会话保持正常。
2. 全局管理员选择租户，租户管理员和审计员权限边界正确。
3. 创建一个测试评估或档案，并完成提交/复核路径。
4. 上传、预览并下载一份测试证据。
5. 账户审计至少验证一个 CSV 数据源导入，或一个启用 TLS 的只读数据库数据源同步。
6. 退出后 refresh session 被撤销。
7. 公网访问 `/api/health/ready` 和 `/api/internal/metrics` 被 Nginx 拒绝。

只有构建、容器健康和上述业务路径都通过，才能认定公网部署完成。

## 7. 备份与异地保存

数据库记录和 uploads 必须作为同一恢复点保护。Compose 的 `backup-tools` profile 会把 PostgreSQL
dump、uploads、manifest 和加密封装放到 `BACKUP_HOST_DIR`。实际备份前应进入维护窗口并停止写流量：

```bash
cd /opt/compliance-platform/app

# dry-run
docker compose --env-file .env --profile ops run --rm backup-tools backup.sh

# 创建真实加密备份
docker compose --env-file .env --profile ops run --rm backup-tools backup.sh --apply
```

加密备份文件和 `.sha256` 应同步到服务器之外；同时单独、安全地保存 `ENCRYPTION_KEY_ID` 和
历史 `ENCRYPTION_KEYRING`，不要把密钥打包进数据备份。

恢复演练只能使用空的临时数据库和空目录：

```bash
docker compose --env-file .env exec -T db \
  createdb -U admin compliance_restore

docker compose --env-file .env --profile ops run --rm \
  -e BACKUP_FILE=/data/backups/<backup-file>.tar.enc \
  -e RESTORE_DB_NAME=compliance_restore \
  -e RESTORE_CONFIRM_DB=compliance_restore \
  -e RESTORE_UPLOAD_DIR=/data/restore-uploads \
  backup-tools restore.sh --apply
```

恢复后要运行迁移、readiness、文件哈希抽样和字段解密验证。演练完成后再显式删除临时数据库和
恢复目录；不要把恢复命令的目标改成在线数据库或在线 uploads volume。

## 8. 日常升级

```bash
cd /opt/compliance-platform/app

# 1. 记录当前提交并完成加密备份/恢复抽检
git rev-parse HEAD

# 2. 获取经过 CI 的目标版本
git fetch --all --prune
git checkout <approved-release-tag-or-commit>

# 3. 构建和执行版本化迁移
./deploy/cloud-preflight.sh .env
docker compose --env-file .env build
docker compose --env-file .env run --rm migrate
docker compose --env-file .env up -d

# 4. 验证
docker compose --env-file .env ps
curl --fail http://127.0.0.1:3001/api/health/ready
curl --fail "https://$DOMAIN/"
```

发生迁移校验和不一致、readiness 503 或核心业务验收失败时，停止发布。代码可切回已批准提交；
数据库是否允许 `migrate:down` 必须按最后迁移及业务数据门禁判断。无法安全回滚时，从已验证备份
恢复到临时环境确认后再切换，不得强行删除迁移记录。

## 9. 运维检查

```bash
docker compose --env-file .env ps
docker compose --env-file .env logs --tail=200 backend
curl --fail http://127.0.0.1:3001/api/health/live
curl --fail http://127.0.0.1:3001/api/health/ready
sudo nginx -t
sudo certbot certificates
```

生产端口、域名、证书、备份目标或密钥轮换发生变化时，应重新执行预检、Nginx 语法校验、
readiness 和浏览器业务验收。
