# 合规管理平台 — 上云部署指南

---

## 一、环境要求

| 组件 | 版本 | 用途 |
|------|------|------|
| Node.js | 18+ | 后端运行时 |
| PostgreSQL | 14+ | 数据库 |
| Nginx | 1.20+ | 反向代理 + 静态文件 |
| PM2 | 5+ | 进程守护 |

---

## 二、云资源

| 资源 | 推荐规格 | 月费参考 |
|------|----------|----------|
| 云服务器 | 2核4G + 40G SSD | ¥80-150 |
| 云数据库 PostgreSQL | 1核2G + 20G | ¥80-120 |
| 域名 + SSL | 1个 .com/.cn | ¥50/年 |

> 最低约 ¥220/月，阿里云/腾讯云均可。

---

## 三、服务器初始化

```bash
# Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# PostgreSQL 客户端 + Nginx + Git
apt-get install -y postgresql-client nginx git

# PM2
npm install -g pm2
```

---

## 四、部署步骤

### 4.1 拉代码

```bash
cd /opt
git clone https://github.com/你的用户名/compliance-management-platform.git
cd compliance-management-platform
```

### 4.2 安装依赖

```bash
cd backend && npm install
cd ../frontend && npm install
```

### 4.3 配置环境变量

创建 `backend/.env`：

```env
PORT=3001
NODE_ENV=production

# 数据库
DB_HOST=xxx.rds.aliyuncs.com
DB_PORT=5432
DB_NAME=audit_platform
DB_USER=audit_admin
DB_PASSWORD=你的数据库密码
DB_CA_CERT=/etc/ssl/rds-ca.pem

# JWT
JWT_SECRET=至少32位随机字符串
JWT_EXPIRES_IN=900
JWT_REFRESH_EXPIRES_IN=86400

# 加密密钥（数据源密码加密用）
ENCRYPTION_KEY=另一个32位随机字符串

# 前端地址
FRONTEND_URL=https://你的域名

# 文件上传
UPLOAD_DIR=/data/uploads

# SMTP（可选）
SMTP_HOST=smtp.yourcompany.com
SMTP_PORT=587
SMTP_USER=noreply@yourcompany.com
SMTP_PASS=
SMTP_FROM=noreply@yourcompany.com
```

生成随机密钥：

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 4.4 初始化数据库

```bash
cd /opt/compliance-management-platform/backend
npx tsx src/config/migrate.ts   # 创建/更新表结构
npx tsx src/config/seed.ts      # 创建默认角色和用户
```

> `migrate.ts` 使用 `sequelize.sync({ alter: true })`，首次运行创建所有表，后续运行增量更新。

### 4.5 构建前端

```bash
cd /opt/compliance-management-platform/frontend
npm run build   # 生成 dist/ 目录
```

### 4.6 启动后端

```bash
cd /opt/compliance-management-platform/backend
npx tsc                                  # 编译 TypeScript
pm2 start dist/index.js --name compliance --env production
pm2 save
pm2 startup                              # 开机自启
```

### 4.7 配置 Nginx

创建 `/etc/nginx/sites-available/compliance`：

```nginx
server {
    listen 80;
    server_name 你的域名;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name 你的域名;

    ssl_certificate /etc/ssl/certs/证书.pem;
    ssl_certificate_key /etc/ssl/private/密钥.key;

    root /opt/compliance-management-platform/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    client_max_body_size 50M;
}
```

启用：

```bash
ln -s /etc/nginx/sites-available/compliance /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx
```

---

## 五、安全检查清单

| # | 检查项 | 验证方式 |
|---|--------|----------|
| 1 | JWT_SECRET 已改为随机值 | `grep JWT_SECRET backend/.env` |
| 2 | ENCRYPTION_KEY 已改为随机值 | `grep ENCRYPTION_KEY backend/.env` |
| 3 | 默认密码已改 | 登录后修改 admin 密码 |
| 4 | .env 未提交到 Git | `.gitignore` 已包含 |
| 5 | HTTPS 生效 | 浏览器显示锁图标 |
| 6 | 防火墙仅开 80/443 | `ufw status` |
| 7 | PM2 开机自启 | `pm2 startup` 已执行 |
| 8 | 数据库自动备份 | 云数据库控制台开启 |

---

## 六、运维命令

| 操作 | 命令 |
|------|------|
| 查看后端日志 | `pm2 logs compliance` |
| 重启后端 | `pm2 restart compliance` |
| 更新代码 | `git pull && cd backend && npm install && tsc && pm2 restart compliance && cd ../frontend && npm install && npm run build` |
| 进程状态 | `pm2 status` |
| 数据库备份 | `pg_dump -h 云库地址 -U 用户名 audit_platform > backup.sql` |
| Nginx 重载 | `nginx -t && systemctl reload nginx` |

---

## 七、数据表说明

首次部署时 `migrate.ts` 自动创建以下表：

| 表 | 说明 |
|----|------|
| `users` | 用户（含 `roleId` 关联角色） |
| `roles` | RBAC 角色（权限 JSONB） |
| `system_settings` | 安全策略配置（登录锁定、空闲超时） |
| `account_data_sources` | 数据源配置 |
| `account_data` | 账户数据（HOT/WARM/COLD） |
| `account_audit_tasks` | 审计任务定义 |
| `account_task_executions` | 任务执行历史 |
| `account_problems` | 问题记录 |
| `audit_rules` | 审计规则 |
| 以及资质合规模块相关表 |

`seed.ts` 预置 3 个角色和 3 个默认用户。
